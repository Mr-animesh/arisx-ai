version: "3.9"

services:
  scraper:
    build: .
    env_file: .env
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./logs:/app/logs
    restart: unless-stopped
    command: python run.py --schedule --interval 24
